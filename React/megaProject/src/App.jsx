
import './App.css'

function App() {
  // console.log(import.meta.env.VITE_APPWRITE_URL); this is how we can access the env variables while using the vite //better way is to use it by creating the conf folder
  
  return (
    <>
      <h1>App with Appwrite</h1>
    </>
  )
}

export default App
