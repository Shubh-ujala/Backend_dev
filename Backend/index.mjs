import chalk from "chalk";
console.log(chalk.blue('Hello, world!'));
console.log(chalk.red.bold('This is an error message.'));
console.log(chalk.green.underline('This is a success message.'));

console.log(chalk.green.underline("hi there!"));

// so this is how we can require an external package!(this is the concept of external )
// and we can also read the functionality in the docs of the chalk package!

